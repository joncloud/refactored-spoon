language: bash

branches:  
  only:
    - master

services:  
    - docker

script:  
    - echo "docker build --no-cache -t joncloud/jdb_ci_test ."
    - echo "docker run -it --rm joncloud/jdb_ci_test"
    - echo $PRIVATE_KEY > .test
    - cat .test
